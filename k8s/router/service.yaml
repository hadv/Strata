apiVersion: v1
kind: Service
metadata:
  name: router-service
  namespace: strata
  labels:
    app: router
spec:
  selector:
    app: router
  ports:
    - port: 8082
      targetPort: 8082
      protocol: TCP
  type: ClusterIP

apiVersion: apps/v1
kind: Deployment
metadata:
  name: fee-service-eu
  namespace: strata
  labels:
    app: fee-service-eu
spec:
  replicas: 1
  selector:
    matchLabels:
      app: fee-service-eu
  template:
    metadata:
      labels:
        app: fee-service-eu
    spec:
      containers:
        - name: fee-service
          image: strata-fee-service:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8090
          env:
            - name: COUNTRY
              value: EU
          resources:
            limits:
              memory: "512Mi"
              cpu: "500m"
---
apiVersion: v1
kind: Service
metadata:
  name: fee-service-eu
  namespace: strata
spec:
  selector:
    app: fee-service-eu
  ports:
    - port: 8090
      targetPort: 8090
